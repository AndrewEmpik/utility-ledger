doctype html
html(lang="uk")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Дані внесено
    style.
      body {
        font-family: Verdana;
        background: #1f1f1f;
        color: #b5bac9;
        font-size: 22px;
        padding: 40px;
        text-align: center;
      }
      .success-message {
        margin-top: 60px;
        font-size: 32px;
        color: #4CAF50;
        font-weight: bold;
      }
      .back-link {
        display: inline-block;
        margin-top: 40px;
        padding: 16px 32px;
        font-size: 20px;
        background-color: #4C50AF;
        color: white;
        border: none;
        border-radius: 8px;
        text-decoration: none;
      }
  body
    div.success-message Дані успішно внесено!
    a.back-link(href="/") На головну 